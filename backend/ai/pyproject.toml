[project]
name = "pricewatch-ai"
version = "10.3.0"
description = "PriceWatch V10.3 AI Advisor gateway with local Ollama and Google Cloud Run providers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.0",
    "google-auth[requests]>=2.40.0",
    "httpx>=0.28.1",
    "pydantic>=2.11.0",
    "pydantic-settings>=2.10.0",
    "uvicorn>=0.35.0",
]

[tool.uv]
package = false
